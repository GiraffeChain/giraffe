name: Push

on:
  push:
    branches:
      - "main"

jobs:
  sbt-build:
    uses: ./.github/workflows/_sbt_build.yml
  publish-docker-images:
    uses: ./.github/workflows/_docker_publish.yml
    needs: [sbt-build]
    secrets: inherit
    with:
      dev_tag: true
      latest_tag: false
